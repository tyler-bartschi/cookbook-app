{
  "name": "@cookbook/server",
  "version": "1.0.0",
  "description": "Server package for the cookbook app, designed to deploy to AWS services.",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run build -w @cookbook/shared && npm run clean && tsc -p tsconfig.json",
    "compile": "npm run clean && npm test && npm run typecheck && npm run lint && npm run build",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ./jest.config.cjs --passWithNoTests"
  },
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1045.0",
    "@aws-sdk/client-s3": "^3.1045.0",
    "@aws-sdk/lib-dynamodb": "^3.1045.0",
    "@cookbook/shared": "*",
    "@types/aws-lambda": "^8.10.161",
    "@types/aws-sdk": "^0.0.42",
    "zod": "^4.4.3"
  }
}
