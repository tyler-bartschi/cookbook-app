{
  "name": "@cookbook/web",
  "private": true,
  "version": "1.0.0",
  "description": "Web package for the cookbook app, uses React and Vite.",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npm run clean && npm test && npm run typecheck && npm run lint && npm run build",
    "dev": "vite",
    "build": "npm run build -w @cookbook/shared && npm run clean && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "test": "jest --config ./jest.config.cjs --passWithNoTests"
  },
  "dependencies": {
    "@cookbook/shared": "*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.4"
  }
}
